import Link from 'next/link';import {MapPin,CalendarDays,ShieldAlert} from 'lucide-react';import {Property,maxBid} from '@/lib/demo';
const money=(n:number)=>'$'+n.toLocaleString();
export default function PropertyCard({p}:{p:Property}){const mb=maxBid(p);return <div className="propertyCard"><div className="cardTop"><span className={'pill '+p.potential.toLowerCase()}>{p.potential} potential</span><span className="county">{p.county}</span></div><h3>{p.address}</h3><p className="muted"><MapPin size={14}/> {p.city}, Delaware</p><div className="metrics"><div><span>Market value</span><b>{money(p.marketValue)}</b></div><div><span>Opening bid</span><b>{money(p.openingBid)}</b></div><div><span>Model max bid</span><b className="accent">{money(mb)}</b></div></div><div className="risk"><ShieldAlert size={16}/><span>{p.titleStatus} · {p.issues} unresolved issue{p.issues===1?'':'s'}</span></div><div className="cardFoot"><span><CalendarDays size={15}/> {p.saleDate}</span><Link href={'/properties/'+p.id}>View intelligence →</Link></div></div>}
