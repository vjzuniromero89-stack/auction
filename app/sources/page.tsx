import Nav from '@/components/Nav';import {OFFICIAL_SOURCES} from '@/lib/official-sources';import {ExternalLink} from 'lucide-react';export default function Page(){return <main><Nav/><section className="content"><p className="eyebrow">EVIDENCE NETWORK</p><h1>Sources & Providers</h1><p className="subtitle">Official verification sources and optional provider integrations.</p><div className="sourceCards">{OFFICIAL_SOURCES.map(s=><a className="sourceCard" key={s.name} href={s.url} target="_blank"><div><b>{s.name}</b><span>{s.kind}</span></div><ExternalLink size={16}/></a>)}</div><div className="panel"><h2>Optional paid integrations</h2><p>Architecture is ready for First American/DataTree, ATTOM and PACER. Credentials are intentionally not required for deployment. PACER automation should only be enabled after adding your PACER credentials and accepting billable searches.</p></div></section></main>}